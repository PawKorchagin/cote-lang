fn main() {
    y = x = 2 + 3 * 4;
    if (y == 3) {
        while (1) {

        }
    }
}
fn _f2() {
// this is a comment
    y = x = 2; // we can write comments!
    if (y == 2) {
        while (y < 2 * 3) {
            y=y+1*x;
        }
    } else if (x == 3) {

    }
    else if (x == 4) {

    }
    else {
        x = 1;
    }
    //      (fn: (x, y) -> x + y + 1) * 3 + 1
    //      fn: (x, y) -> x + y + 1 * 3 + 1
    x = y = fn(a) { x + y + 1; } * 3 - 1;
    x = y = fn() { x + y + 1; } * 3 - 1;
    x = y = fn(a) { x + y + 1; };
    x = y = fn(a, b) { x + y + 1; };
    x = y = fn(a,b,c) { x + y + 1; x(1, x * 2 + 3 * y); };
    z = y * x + 2 * 2;
}
