fn main() {
    y = x = 2 + 3 * 4;
    if (y == 3) {
        while (1) {

        }
    }
}

fn _f2() {
// this is a comment
    y = x = 2; // we can write comments!
    if (y == 2) {
        while (y < 2 * 3) {
            y=y+1*x;
        }
    } else {
        x = 1;
    }
    //      (fn: (x, y) -> x + y + 1) * 3 + 1
    //      fn: (x, y) -> x + y + 1 * 3 + 1
    x = y = fn: () -> { x + y + 1; } * 3 - 1;
    z = y * x + 2 * 2;
}
